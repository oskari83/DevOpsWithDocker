# Getting Started 

To configure
npm install
npm install cypress
npm install eslint-plugin-cypress

To run development:
npm start

To run build
npm install -g serve
npm run build
serve -s -l 3000 build